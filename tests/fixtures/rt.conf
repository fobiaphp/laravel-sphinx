index rt {
    type          = rt
    path          = index_test_rt

    morphology   = stem_enru, soundex, metaphone
    min_stemming_len        = 3
    expand_keywords         = 1
    index_exact_words       = 1
    min_infix_len           = 3
    min_word_len            = 3

    # attributes
    rt_field     = name
    rt_field     = content

    rt_attr_uint  = gid
    rt_attr_float = greal
    rt_attr_bool  = gbool

    rt_attr_string = name

    rt_attr_multi = tags
    rt_attr_json  = factors
}
